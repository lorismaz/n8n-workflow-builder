export const N8N_HOST = process.env.N8N_HOST || '';
export const N8N_API_KEY = process.env.N8N_API_KEY || '';

// GitHub token for API access (optional)
export const GITHUB_TOKEN = process.env.GITHUB_TOKEN || ''; // Optional personal access token for higher rate limits
